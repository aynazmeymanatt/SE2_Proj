apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-service
  namespace: ticketing-platform
spec:
  replicas: 2
  selector:
    matchLabels: { app: catalog-service }
  template:
    metadata:
      labels: { app: catalog-service }
    spec:
      containers:
        - name: catalog-service
          image: ticketing-platform/catalog-service:latest
          ports:
            - containerPort: 3003
          env:
            - name: PORT
              value: "3003"
          readinessProbe:
            httpGet: { path: /health, port: 3003 }
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests: { cpu: "50m", memory: "64Mi" }
            limits: { cpu: "250m", memory: "128Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: ticketing-platform
spec:
  selector: { app: catalog-service }
  ports:
    - port: 3003
      targetPort: 3003
