apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-service
  namespace: ticketing-platform
spec:
  replicas: 2
  selector:
    matchLabels: { app: identity-service }
  template:
    metadata:
      labels: { app: identity-service }
    spec:
      containers:
        - name: identity-service
          image: ticketing-platform/identity-service:latest
          ports:
            - containerPort: 3002
          env:
            - name: PORT
              value: "3002"
          readinessProbe:
            httpGet: { path: /health, port: 3002 }
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests: { cpu: "50m", memory: "64Mi" }
            limits: { cpu: "250m", memory: "128Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: ticketing-platform
spec:
  selector: { app: identity-service }
  ports:
    - port: 3002
      targetPort: 3002
